import { SuperPerforatorDecorator } from './super-perforator.decorator';

describe('SuperPerforator.Decorator', () => {
  it('should create an instance', () => {
    expect(new SuperPerforatorDecorator()).toBeTruthy();
  });
});
